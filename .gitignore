node_modules/
dist/
app/dist/
.DS_Store
.env
*.log
.wrangler/
.wake/
.wake-dogfood/
# Internal strategy/loop notes — private operational context, never published.
.context/

# Local research scratch; curate deliberately before publishing.
experiments/*
# ...except intentionally-published spikes:
!experiments/terrarium-airlock/

# Guardrail deploy receipts are local tool output, not repo content.
.guardrail/

# Untracked local research notes that reference internal-only hostnames/links.
# These are operational scratch, not curated public docs; keep them local until
# deliberately sanitized and committed.
docs/DRIFT_RESEARCH_INTERNAL.md
docs/DRIFT_RESEARCH_EXTERNAL.md
docs/DRIFT_RESEARCH_PORTFOLIO.md
docs/CLOUD_NORTHSTAR_RESEARCH.md

# Operational eval run outputs (internal model names / hostnames) — keep local.
artifacts/accuracy-bench/
artifacts/cloud-scale-eval/raw-*.jsonl
artifacts/cloud-scale-eval/summary-*.json
artifacts/cloud-scale-eval/vote-*

# Rust/WASM build artifacts
wasm-verifier/target/

# terrarium-airlock spike run output
experiments/terrarium-airlock/out/

# Cloud proof/evidence run artifacts (receipts, logs, verdicts, backups) — local
# operational output, not repo content. The re-runnable scripts stay tracked.
artifacts/cloud-proof/
artifacts/cloud-scale-eval/*.md
artifacts/terrarium-fanout-demo.cast
artifacts/terrarium-fanout-result.json

# Local runner runtime install dir (has its own package.json/lock).
scripts/opencode-runtime/

# Internal handoff/redteam notes that cite .context/ loops or internal-only
# context — keep local until deliberately sanitized (same policy as the
# DRIFT_RESEARCH_*/CLOUD_NORTHSTAR_RESEARCH docs above).
docs/PULSE-HANDOFF-2026-06-27.md
docs/CLOUD_NORTHSTAR_REDTEAM.md
docs/DRIFT_REDTEAM.md
docs/DRIFT_EXPERIMENTS.md

# Drift-experiment run output (raw receipts + logs, internal markers) — internal
# ops evidence, kept local. The re-runnable drift-lab scripts stay tracked.
docs/evidence/
