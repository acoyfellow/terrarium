{
  "name": "pulse",
  "version": "0.0.1",
  "private": true,
  "description": "Durable callback plumbing for delegated work: emit, journal, dedupe, fan out, claim, ack, requeue, status.",
  "type": "module",
  "bin": {
    "pulse": "./src/cli.js"
  },
  "scripts": {
    "start": "node src/serve.js",
    "test": "node --test test/*.test.js",
    "build": "vite build",
    "dev": "vite",
    "e2e:docs": "node scripts/e2e-docs.mjs"
  },
  "dependencies": {
    "hono": "^4.12.0",
    "@hono/node-server": "^1.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.0.0",
    "svelte": "^5.0.0",
    "vite": "^7.0.0"
  }
}
